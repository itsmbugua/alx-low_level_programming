#include <stdio.h>
/**
 * main - program that prints the alphabet
 *
 * Return: always 0
 */
int main(void)
{
	int s;

	for (s = 0; s < 26; s++)
	{
		putchar('a' + s);
	}
	putchar("\n")
		return (0)
}
