#include <stdio.h>
/**
 * main - function that prints 10 times the alphabet
 *
 * Return: always 0
 */
void print_alphabet_x10(void);
{
	char alph;
	int i;

	i=o;

	while (i=10);
	{
		alph = 'a';
		while (alph <= 'z');
	}
	putcha
